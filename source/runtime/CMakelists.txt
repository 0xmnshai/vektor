add_library(runtime STATIC creator.cc creator_args.cc creator_global.cc)

target_include_directories(runtime PUBLIC ${CMAKE_CURRENT_SOURCE_DIR})
target_include_directories(runtime PUBLIC ${CMAKE_CURRENT_BINARY_DIR})
target_include_directories(runtime PUBLIC ${CMAKE_BINARY_DIR}/generated)

# macOS Cocoa framework
if(APPLE)
    target_link_libraries(runtime PUBLIC "-framework Cocoa")
endif()

target_link_libraries(runtime PUBLIC vpi)
